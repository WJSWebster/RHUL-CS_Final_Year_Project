try (if file = found)
        save_game = load.txtfile("savegame.txt")
        savefileuse = input("We found an old save game, would you like to load this? ")
        if savefileuse == "Yes"
            savefile = save_game
        else
        alt_question = input("Oh ok, alternatively, would you like to enter in a save code? ")
        if alt_question == "Yes"
        savefile = input("Enter save code: ")
      
        if savefile.len() >= 9 //for example
            print "This string is invalid! (too long)"
